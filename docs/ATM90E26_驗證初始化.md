
正確的驗證流程應該是：
1.  **讀取目標暫存器**：讀取 `SysStatus` (01H)。
2.  **讀取驗證暫存器**：緊接著讀取 `LastData` (06H)，該暫存器會儲存上一次 SPI/UART 交易的數據。
3.  **比對數值**：比對步驟 1 與步驟 2 取得的數值。若兩者一致，代表通訊正確；若不一致，應以 `LastData` 的值為準，因為它鎖定了晶片送出時的原始狀態。

這種做法能防止因通訊干擾導致讀值錯誤，同時避免因為讀取動作清除了旗標而導致警報遺漏。
